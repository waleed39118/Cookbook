<!DOCTYPE html>
<html>
<head>
  <title>My Applications</title>
</head>
<body>
  <h1>My Applications</h1>

  <% if (userId) { %>
    <a href="/users/<%= userId %>/applications/new">
      <button>+ New Application</button>
    </a>
  <% } else { %>
    <p style="color: red;">User ID not available — cannot create new application.</p>
  <% } %>

  <ul>
    <% if (applications.length > 0) { %>
      <% applications.forEach(app => { %>
        <li><strong><%= app.title %></strong> - <%= app.status || 'Pending' %></li>
      <% }) %>
    <% } else { %>
      <li>No applications found.</li>
    <% } %>
  </ul>
</body>
</html>
